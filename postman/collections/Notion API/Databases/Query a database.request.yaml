$kind: http-request
url: https://api.notion.com/v1/databases/:id/query
method: POST
headers:
  Authorization: Bearer secret_t1CdN9S8yicG5eWLUOfhcWaOscVnFXns
  Content-Type: application/json
  Notion-Version: "{{NOTION_VERSION}}"
pathVariables:
  id: "{{DATABASE_ID}}"
body:
  type: text
  content: |-
    {
        "filter": {
            "property": "Status",
            "select": {
                "equals": "Reading"
            }
        }
    }
examples: ./.resources/Query a database.resources/examples
order: 2000
