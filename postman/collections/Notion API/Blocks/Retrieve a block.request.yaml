$kind: http-request
url: https://api.notion.com/v1/blocks/:id
method: GET
headers:
  Notion-Version: "{{NOTION_VERSION}}"
pathVariables:
  id: "{{BLOCK_ID}}"
examples: ./.resources/Retrieve a block.resources/examples
order: 4000
